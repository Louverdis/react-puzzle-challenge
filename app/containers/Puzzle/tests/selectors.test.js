// import { fromJS } from 'immutable';
// import { makeSelectBoardDomain } from '../selectors';

// const selector = makeSelectBoardDomain();

describe('makeSelectBoardDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
